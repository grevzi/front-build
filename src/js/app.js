import './core/sprite'

window.addEventListener('DOMContentLoaded', () => {

    // import(
    //     /* webpackChunkName: "BaseComponent" */
    //     /* webpackPrefetch: true */
        // './components/NewComponent.js')
        // .then(({default: NewComponent}) => {
        //     new NewComponent().init()
            // let $el = document.querySelector(`[data-insert=${e.target.dataset.target}]`)
            // if ($el) {
            //     (new NewComponent($el)).init().render();
            // }
        // })
        // .catch(error => console.error(`There is some errors in component load [ NewComponent ]`, error.message))

    console.log('App initialized')

})